<UserControl x:Class="ShortestCycleDirGraph.Pages.Input.EdgeSet"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Style="{StaticResource ContentRoot}">
        <ScrollViewer>
            <StackPanel MinWidth="200">
                <TextBlock Text="Wprowadź zbiór krawędzi" Style="{StaticResource Heading2}" />
                <TextBlock Text="Wymagany format to np: 1->2, 2->3, 1->3" Margin="0,10,0,0" />
                <RichTextBox x:Name="EdgeSetControl" Height="200" Width="200" HorizontalAlignment="Left" Margin="0,30,0,0">
                    <FlowDocument>
                        <Paragraph>
                            <Run Text=""/>
                        </Paragraph>
                    </FlowDocument>
                </RichTextBox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
